# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.18.4
loom_version=1.15-SNAPSHOT

# Mod Properties
mod_version=2.2.2
maven_group=com.enhancedmovement.kestalkayden
archives_base_name=enhanced-movement

# Dependencies
fabric_version=0.152.1+26.2

# NeoForge
neoforge_version=26.2.0.0-beta
moddev_version=2.0.141
# NeoForge build/publish gate - set false to hold off when NeoForged lags an MC version.
neoforge_enabled=true

# Config UI (Fabric only — native config screen; NeoForge uses IConfigScreenFactory)
modmenu_version=20.0.0-beta.2
