## Changed
- **Removed a redundant log line.** The mod wrote an INFO line to the server console on every player join (`Player <name> joined with Enhanced Movement mod`). It served no purpose beyond a startup-style confirmation and just added noise to multiplayer server logs, so it's gone. No gameplay, config, or behavior change.
